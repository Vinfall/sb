rules:
  unpinned-uses: # intended by upstream
    ignore:
      - issue.yml
      - issue_comment.yml
      - pr.yml
      - update.yml
      - test.yml:30
  dangerous-triggers: # almost always used insecurely, metigated w/ repo protection
    ignore:
      - pr.yml:1
  bot-conditions:
    ignore:
      - pr.yml:11
